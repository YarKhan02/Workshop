// Test file to verify common components imports
import { Pagination, SearchBar, StatsGrid, PageHeader } from '../components/common';

console.log('Imports successful:', { Pagination, SearchBar, StatsGrid, PageHeader });
